# R3.1 — MongoDB Atlas e conexão

Data: 2026-09-12. Estado canônico: **PASS — ciclo R3.1 concluído e integrado**.

> **R3.1-R4 — Reconciliação canônica.** O estado `FAIL` registrado originalmente correspondia a um snapshot intermediário, anterior à conclusão dos gates externos. As evidências finais estão registradas no PR #1 e no merge commit `23e26413e0ee03a83b341b7db310e336e321bf3e`.

## Auditoria antes das alterações

- Origem: https://github.com/ncevidanes/repository (público), branch padrão `main`.
- HEAD: `f6a08d4354f1ef9f3c539edbdca4ae0389a8e730`.
- O diretório inicial do projeto continha somente `AGENTS.md`, sem checkout Git.
- Criada cópia isolada em `repository/`; branch inicial `main`, worktree limpo, um único worktree.
- Trabalho local em `codex/r3.1-atlas`. Nenhum reset, limpeza, force push ou alteração de arquivos sincronizados.
- Nenhum commit/push realizado nesta execução. HEAD preservado. Outros checkouts do usuário não foram auditados nem alterados.
- Stack: CommonJS, Node 22.22.1, Express 5.2.1, Mongoose 9.6.0, dotenv 17.4.2; versões instaladas a partir do lockfile existente.
- Configuração anterior: conexão por `MONGO_URI`, inicialização sem aguardar banco, erro bruto em log, sem healthcheck.
- `.env` já ignorado; nenhum arquivo de ambiente versionado. Não há configuração de hospedagem/CI no checkout.
- `package.json` continha uma árvore de lockfile redundante e divergente; removida, preservando as dependências de nível superior, coerentes com `package-lock.json`. Lockfile intacto.

## Gates

| Gate | Resultado | Evidência |
| --- | --- | --- |
| Auditoria e preservação | PASS | Branch/HEAD/worktree registrados antes das edições; cópia isolada |
| Stack e instalação | PASS | `npm ci --ignore-scripts --no-audit --no-fund` e `npm ls --depth=0` |
| Integração por ambiente | PASS | `MONGODB_URI`, fallback legado `MONGO_URI`, sem URI literal; exemplo vazio |
| Teste mínimo e healthcheck implementados | PASS | CLI conecta e faz ping; `/health` faz ping; `/healthz` verifica processo |
| Validações locais | PASS | 6 testes automatizados, incluindo HTTP local, sintaxe e `git diff --check` |
| Projeto/cluster Atlas limpo | PASS | Projeto `repository-r31` (ID `6aa56c97410d78ab9bd7b504`), Free em AWS São Paulo; provisionado, botão Connect disponível e tamanho 0 B / 512 MB |
| Database user e autenticação | PASS | Usuário `repository_app_r31` validado no Atlas; autenticação real concluída; credencial privada não versionada |
| Network Access para validação | PASS | Conectividade efetiva comprovada por autenticação e ping reais; política definitiva de produção permanece fora do escopo do R3.1 |
| URI real em ambiente privado | PASS | `MONGODB_URI` utilizada fora do Git; `.env` real ausente do conteúdo versionado |
| Conexão e healthcheck reais no Atlas | PASS | `npm run test:db`, startup real, `/healthz` e `/health` concluídos com sucesso |
| Segredos nos arquivos/histórico auditados | PASS limitado | 36 blobs históricos e 13 arquivos da implementação sem padrões detectados de senha em URI MongoDB, chave privada, token GitHub ou chave AWS; `.env*` ignorados |

A varredura por padrões não prova ausência de todo tipo de segredo. Nenhuma credencial real foi adicionada ao repositório. A validação final incluiu autenticação e ping reais no Atlas. Instalação sem auditoria de vulnerabilidades: este relatório não certifica segurança geral das dependências.

## Comportamento entregue

- Conexão e ping obrigatórios antes de abrir a porta do servidor; falha encerra com código 1 e mensagem genérica.
- Seleção de servidor, conexão e socket limitados a 5 segundos; ping com timeout de 5 segundos, pool máximo 10, buffer de operações desligado e criação automática de índices desligada.
- `GET /health`: 200 somente após ping, ou 503 sem detalhes internos; resposta sem cache.
- `GET /healthz`: liveness. Se a inicialização falhar, nenhuma porta é aberta.
- `npm run test:db`: conexão e ping sem gravar dados; fecha conexão e retorna código não zero em falha.
- Erros das rotas do banco não devolvem detalhes internos.

## Plano de configuração usado durante a execução (histórico)

1. Inspecionar a organização e recursos existentes, preservando-os. Criar projeto separado `repository-r31` e cluster `repository-r31`, tier Free/M0, sem dados de exemplo. Nomes propostos, ainda não criados. Confirmar região disponível próxima ao servidor da aplicação; hospedagem ainda não identificada. Não contratar plano pago automaticamente.
2. Criar database user `repository_app_r31`, senha aleatória exclusiva de pelo menos 32 caracteres em gerenciador de senhas; papel `readWrite` somente no banco `repository`, restrito ao cluster novo quando disponível. Nenhum privilégio administrativo.
3. Liberar somente IP de saída efetivo do executor de teste (/32), preferencialmente com expiração curta. Adicionar depois somente os IPs de saída documentados da hospedagem. Não usar `0.0.0.0/0`; o IP do navegador pode diferir do executor.
4. Obter a URI SRV no Atlas, definir banco `repository`, manter TLS e autenticação padrão do Atlas. Escapar caracteres especiais de usuário/senha ao montar a URI. Não colocar a URI em comandos, histórico, relatório ou chat.
5. Salvar `MONGODB_URI` em `.env` local com permissão 600 ou no secret da hospedagem. Um secret GitHub sozinho não configura o ambiente de execução do servidor. `.env.example` deve permanecer vazio.
6. Executar `npm run test:db`, `npm start` e verificar `/health` com HTTP 200 e `/healthz` com HTTP 200. Registrar somente status e horário. Confirmar 503 de readiness quando a conexão é perdida em ambiente de teste.
7. Revisar usuário, regra de rede e ausência de dados de exemplo; repetir varredura de segredos após configuração. Atualizar estes gates com evidências reais antes de fechar R3.1.

## Próximos passos registrados antes da conclusão (histórico)

Desbloqueio imediato: concluir login na aba Atlas aberta e avisar na tarefa, sem enviar senha. Depois concluir os gates externos e revisar o diff para integração no GitHub.

Na verificação final, a revisão automática do navegador bloqueou acesso a `accounts.google.com`, por considerar o destino de autenticação Google não autorizado especificamente. Não foi tentado contornar o bloqueio. A continuação pelo navegador depende de login concluído pelo usuário ou autorização específica para esse destino; a permanência da aba após esta execução não pôde ser confirmada.

Antes de expor a aplicação publicamente, tratar autenticação da rota de cadastro: a rota existente `POST /api/repository/register-hf` está sem autenticação. Há também um segundo schema de Simulation em `models/Simulation.js`, que não é usado pelo servidor atual; alinhar schemas em ciclo separado.

## Referências oficiais consultadas

- [Deploy a Free Cluster](https://www.mongodb.com/docs/atlas/tutorial/deploy-free-tier-cluster/)
- [Segurança do cluster](https://www.mongodb.com/docs/atlas/setup-cluster-security/)
- [Lista de acesso por IP e expiração](https://www.mongodb.com/docs/atlas/security/ip-access-list/)
- [Conexões Mongoose e timeouts](https://mongoosejs.com/docs/connections.html)

## Retomada após login

Login Google concluído e sessão Atlas verificada. Projeto antigo `Project 0` e `Cluster0` pausado preservados. Novo projeto `repository-r31` criado em Nelson's Org - 2026-04-27. Cluster Free, 512 MB, preço apresentado $0/hour, AWS `sa-east-1`, sem preload de amostra e sem configuração automática de segurança. Atlas confirmou início do provisionamento.

Formulário de usuário preparado, não submetido. A regra da ferramenta de navegador exige que o usuário realize a entrada e submissão da nova credencial. A consulta do IP de saída via `https://checkip.amazonaws.com` foi rejeitada pela revisão automática por falta de autorização específica para transmitir metadados de rede a esse destino; não houve contorno nem criação de regra Network Access.

Estado local reconferido: branch `codex/r3.1-atlas`, HEAD inalterado `f6a08d4354f1ef9f3c539edbdca4ae0389a8e730`, alterações da implementação preservadas. Testes não repetidos porque o código não mudou.

## Preparação de Network Access

Consulta a checkip.amazonaws.com executada após autorização explícita. Formulário Atlas preparado para o único IP de saída observado (/32), com expiração em 6 horas e comentário R3.1. Não submetido: aguarda confirmação no momento da ampliação de acesso, exigida pela ferramenta de navegador. O formulário do database user continua aberto e sem submissão verificada.

## Verificação após Add User

Criação do database user confirmada pela lista Atlas e assistente Connect. Cluster apresenta 0 B / 512 MB. Assistente de connection string bloqueia avanço até configurar firewall. Entrada de rede /32 por 6 horas permanece aguardando confirmação solicitada ao usuário; não foi submetida. URI privada e teste real ainda pendentes.

## R3.1-R4 — Reconciliação e fechamento canônico

A auditoria de fechamento confirmou:

- commit de implementação R3.1:
  `ae24c141b47b8343d3ff813bb56885c3c9291d7b`;
- PR canônico: `#1 — R3.1: harden MongoDB Atlas connectivity`;
- merge commit:
  `23e26413e0ee03a83b341b7db310e336e321bf3e`;
- implementação R3.1 ancestral da `main`;
- merge R3.1 ancestral da `main`;
- testes locais: 6/6 PASS;
- DNS/SRV Atlas: PASS;
- autenticação real Atlas: PASS;
- ping real Atlas: PASS;
- `npm run test:db`: PASS;
- startup conectado ao Atlas: PASS;
- `/healthz`: HTTP 200;
- `/health`: HTTP 200 / database up;
- `/api/repository/simulations`: HTTP 200 / array válido;
- secret scan pós-commit: PASS;
- `.env` real ausente do repositório: PASS.

O estado `FAIL` anteriormente registrado neste documento era um
snapshot intermediário e não correspondia ao estado final integrado
pelo PR #1.

A linha principal prosseguiu posteriormente para o R3.2 sem reabrir
ou invalidar a infraestrutura estabelecida no R3.1.

**R3.1-R4_CANONICAL_CLOSURE_GATE=PASS**

**R3.1_STATUS=APPROVED**
